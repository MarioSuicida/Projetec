import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SalvatransacaoPage } from './salvatransacao.page';

describe('SalvatransacaoPage', () => {
  let component: SalvatransacaoPage;
  let fixture: ComponentFixture<SalvatransacaoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SalvatransacaoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
